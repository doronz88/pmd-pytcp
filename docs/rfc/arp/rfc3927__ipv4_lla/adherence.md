# RFC 3927 — IPv4 Link-Local Autoconfig (superseded)

| Field            | Value                                                                                     |
|------------------|-------------------------------------------------------------------------------------------|
| RFC number       | 3927                                                                                      |
| Title            | Dynamic Configuration of IPv4 Link-Local Addresses                                        |
| Canonical record | [`docs/rfc/ip4/rfc3927__ip4_link_local/adherence.md`](../../ip4/rfc3927__ip4_link_local/adherence.md) |
| Status           | **superseded by canonical record** (2026-05-12)                                           |

This record was authored before the RFC 3927 implementation
track existed and described a "no LLA path" state. As of
2026-05-12 the RFC 3927 client is shipped (Phases 0-5,
commits `a845d5a1` → `8f09846b`); the canonical refreshed
adherence record now lives under `docs/rfc/ip4/` alongside
the other IPv4 RFC audits — that is the right family because
RFC 3927 governs IPv4 addressing (the ARP / ACD aspect it
relies on is audited separately under
[`../rfc5227__ipv4_acd/adherence.md`](../rfc5227__ipv4_acd/adherence.md)).

**Do not edit this file.** Future RFC 3927 audit refreshes
go to the canonical record:

➡️ [`docs/rfc/ip4/rfc3927__ip4_link_local/adherence.md`](../../ip4/rfc3927__ip4_link_local/adherence.md)

The ARP-folder location is retained as a discovery aid (a
reader searching the ARP folder for IPv4 link-local content
finds this pointer) but holds no authoritative adherence
content.

## Why the original audit was wrong

The pre-implementation text listed several "principal gaps"
— all closed by the RFC 3927 implementation track:

| Original "gap" claim                                            | Current state                                       | Closed by                                        |
|-----------------------------------------------------------------|-----------------------------------------------------|--------------------------------------------------|
| §1.9 / §2.11 DHCP-fallback coordination missing                 | `Ip4LinkLocal._reconcile_with_dhcp`                 | `4c7cbc49` (Phase 4)                             |
| §2.1 MAC-seeded RNG candidate generation missing                | `link_local__rng.candidate_from_mac`            | `b48d7fc3` (Phase 1)                             |
| §2.2 ARP Probe / §2.4 Announce wire path missing                | Via `Ip4AddressApi.claim_with_acd`                  | `6970c042` (Phase 0.5) + `e6f88f9c` (Phase 2)    |
| §2.5 defend / abandon decision tree missing                     | `Ip4LinkLocal._on_bound_conflict`                   | `4f2935f6` (Phase 3)                             |
| §2.6 TX-source scope-mismatch gate missing                      | `_phtx_ip4` scope check + DROPPED counter           | `a845d5a1` (Phase 0)                             |
| §9 MAX_CONFLICTS / RATE_LIMIT_INTERVAL                          | `link_local__constants` + claim loop            | `b48d7fc3` (Phase 1) + `e6f88f9c` (Phase 2)      |

The canonical record at
`docs/rfc/ip4/rfc3927__ip4_link_local/adherence.md` records
every §-section as met against the shipped implementation,
with full test-coverage audit.
