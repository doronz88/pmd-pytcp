# Archived planning docs

Completed or superseded planning / ledger documents, kept for
historical reference. The work each describes has shipped (or the
problem it tracked no longer exists), and none is linked from the
active `.claude/rules/`, the running code, the per-RFC adherence
records under `docs/rfc/`, or the session memory index.

For the design rationale of shipped subsystems, prefer the live
documents in the parent [`docs/refactor/`](../) directory and the
per-RFC adherence records under [`docs/rfc/`](../../rfc/).

| Document | What it tracked |
|----------------------------------|--------------------------------------------------------|
| `udp_remaining_items.md`         | UDP host-requirements follow-up list (closed).         |
| `v3_0_6_release_readiness.md`    | 3.0.6 release-readiness checklist.                     |
| `v3_0_6_remaining_work.md`       | 3.0.6 remaining-work ledger (all items closed).       |
| `audit_g_test_docstring_sweep.md`| One-off §7.2 test-docstring audit sweep (closed).      |
